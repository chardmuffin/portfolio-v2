import React from 'react';
import ProjectList from "../ProjectList";

function Projects() {

    return (
        <section>
            <h1>My Projects</h1>
            <p>Select any of the projects below to see a preview.</p>
            <ProjectList></ProjectList>
        </section>
    );
};

export default Projects;